# Import library yang diperlukan
import cv2
import numpy as np
import matplotlib.pyplot as plt

# Langkah 1: Upload gambar ke Google Colab
from google.colab import files
uploaded = files.upload()

# Langkah 2: Baca gambar menggunakan OpenCV
image_path = list(uploaded.keys())[0]  # Ambil nama file yang diupload
image = cv2.imread(image_path)
image_gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)  # Konversi ke grayscale

# Langkah 3: Tambahkan noise Gaussian ke gambar
mean = 0
sigma = 25
noisy_image = image_gray + np.random.normal(mean, sigma, image_gray.shape).astype(np.uint8)

# Langkah 4: Terapkan filter Gaussian dan Mean Filter
gaussian_smoothed = cv2.GaussianBlur(noisy_image, (5, 5), 0)
mean_smoothed = cv2.blur(noisy_image, (5, 5))

# Langkah 5: Tampilkan hasil perbandingan
plt.figure(figsize=(15, 5))

# Gambar dengan noise
plt.subplot(1, 3, 1)
plt.imshow(noisy_image, cmap='gray')
plt.title("Gambar dengan Noise")
plt.axis('off')

# Gambar hasil Gaussian Filter
plt.subplot(1, 3, 2)
plt.imshow(gaussian_smoothed, cmap='gray')
plt.title("Gaussian Filter")
plt.axis('off')

# Gambar hasil Mean Filter
plt.subplot(1, 3, 3)
plt.imshow(mean_smoothed, cmap='gray')
plt.title("Mean Filter")
plt.axis('off')

plt.tight_layout()
plt.show()
