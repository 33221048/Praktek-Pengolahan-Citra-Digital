# Import library yang diperlukan
import cv2
import matplotlib.pyplot as plt

# Langkah 1: Upload gambar ke Google Colab
from google.colab import files
uploaded = files.upload()

# Langkah 2: Baca gambar menggunakan OpenCV
image_path = list(uploaded.keys())[0]  # Ambil nama file yang diupload
image = cv2.imread(image_path)

# Langkah 3: Konversi format BGR ke RGB (karena OpenCV membaca dalam BGR)
image_rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

# Langkah 4: Tampilkan gambar menggunakan matplotlib
plt.imshow(image_rgb)
plt.title("Gambar dengan OpenCV")
plt.axis('off')  # Matikan sumbu x dan y
plt.show()
